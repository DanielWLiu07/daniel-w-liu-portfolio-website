# Casino delivery assets

Keep authored inputs and the older delivery files intact. The public scene uses:

- `KatieRoze-display-512.woff2`: generated by `python3 scripts/optimize-casino-display-font.py`. All glyphs, advance widths and outlines retained; the 911-ppem embedded bitmap strike becomes 512 ppem with a 256-entry RGBA palette. This is a visual delivery tradeoff, not lossless compression. Current canvas lettering is drawn at 130–190 px. Original 911-ppem artwork remains in `KatieRoze-web.woff2`.
- `watercolor_normal-delivery.webp`: `node scripts/optimize-casino-delivery.mjs`. Same 1024×1024 dimensions, near-lossless WebP, measured RGB RMSE below 2/255. Authored PNG and lossless WebP remain available.
- Dealer: run `node scripts/optimize-casino-dealer.mjs --delivery` then `node scripts/compress-casino-dealer.mjs --delivery`. Texture uses WebP86 at its original 2048×2048 size (RGB/alpha RMSE3.49/255). Meshopt now includes sparse morph-target indices/values: 232 compressed views, checked byte-exact with both encoder-package and shipped runtime decoders. No geometry simplification or animation/rigging changes. Raw GLB3.94→2.99MB; gzip estimate2.95→2.42MB. Intermediate delivery GLB/manifest stay local; authored, compact and lossless sources are retained.

The resume JPEG and PDF are deliberately unchanged: lossless WebP conversion of the JPEG was larger, not smaller.

Verification: production build, loading gate/card-depth/responsive/dealer-entrance checks; compare browser-rendered font samples, full scene and clean-profile Chrome runs. Test fresh browser profiles with cache disabled and cookies cleared, without seeding quality preferences. For bandwidth comparisons use identical 5 Mbps / 80 ms latency settings. Do not compare localhost timings to production as a measured production speedup. Browser cleanup does not reset operating-system or GPU-driver caches.

Runtime changes: skip the hidden dealer's pose evaluation before impact, retain offscreen scene MSAA but omit redundant fullscreen-canvas MSAA, and load the legacy video route only when selected. None of these changes bypass the ready gate or start the intro before preparation completes.

## Covered shader compilation (September 15, 2026)

`compile-scene.ts` retains Three r185's sequential node construction and cooperative yields, but overlaps up to eight GPU pipeline promises with a sliding window. It drains every job before returning and restores the backend method, including on failure. Only covered precompilation uses this adapter; ordinary rendering is unchanged. Other Three revisions and WebGL use the stock compiler. Revalidate the adapter when upgrading Three; r185 contains the shared pipeline-descriptor reset fix: https://github.com/mrdoob/three.js/pull/33794.

The full covered compositor render and GPU readback fence remain mandatory. Removing the separate precompile was tested and rejected: more main-thread blocking and a late pipeline after reveal.

Matched local production-build stress tests, three fresh Chrome profiles per version: original ready times 8.84/8.61/8.45 seconds; eight-job window 6.73/5.88/6.31 seconds. Median 8.61→6.31 seconds (27% reduction). These are controlled compilation tests, not deployed timings or a guarantee for Safari/phones. The harness disables browser HTTP/shader disk caches and Dawn blob caching, renames shader entry points, and adds a per-run sub-millionth clip-space vertex offset to force fresh vertex compilation without materially changing the image. This perturbation exists ONLY in the browser test harness, never website code; it does not reset every OS/driver cache or invalidate every fragment shader. Plain fresh-profile repeats cannot establish a true cold-GPU speedup on their own.

Regression checks: `npx tsx scripts/check-compile-scene.ts`, `npx tsx scripts/check-scene-warmup.ts`, `npx tsx scripts/check-casino-intro-ready.ts`, and `npx tsx scripts/check-card-render-layers.ts`. Browser checks must also watch for GPU validation errors and new pipeline creation throughout the full intro, not just successful loading.
