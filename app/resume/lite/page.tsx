'use client'

// The original resume page (desk scene), kept intact as the low-power path.
// /resume renders it automatically in Lite mode; this route reaches it directly.
export { default } from '../legacy'
